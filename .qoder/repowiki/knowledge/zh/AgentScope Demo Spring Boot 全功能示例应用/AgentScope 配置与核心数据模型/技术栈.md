Spring Boot 3.5 条件装配（`@Profile`）；AgentScope 2.0 `io.agentscope.core.model.ModelRegistry` + DashScope 扩展（`DashScopeChatFormatter`、`ReActAgent`、各后端 `AgentStateStore`）；Lombok `@Getter`；Jackson `@JsonInclude(NON_NULL)` 用于 SSE payload 序列化。