运行前需设置环境变量 `DASHSCOPE_API_KEY`；不同能力通过激活 Spring profile 启用：`--spring.profiles.active=a2a` 打开 A2A 服务，`--spring.profiles.active=agui` 启用 `/ag-ui/run` 端点，`--spring.profiles.active=feishu` 接入飞书通道，数据库场景使用 `mysql` / `postgresql` / `redis` profile 注入对应 StateStore。