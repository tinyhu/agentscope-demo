`pip install .` 安装后可直接运行 `cli-anything-agentscope`；也可 `python -m cli_anything.agentscope` 启动 REPL。默认连接 `http://localhost:8080`，可通过 `--server` 选项或设置 `AGENTSCOPE_BASE_URL` 环境变量覆盖目标服务器地址。无服务器时多数命令会给出 "Start with: mvn spring-boot:run" 提示。