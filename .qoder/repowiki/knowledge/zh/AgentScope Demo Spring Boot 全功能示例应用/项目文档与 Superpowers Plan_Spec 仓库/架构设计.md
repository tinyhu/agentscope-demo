该模块为纯文档集合，无可执行代码；按职责划分为两个子目录：
- `supervisor-shared-blackboard-design.md`：针对 Supervisor/Router + Shared Blackboard 的单篇架构设计，定义 Conversation AgentState、Shared Blackboard、Expert Private State 三层状态边界及其 key、寻址维度与数据流时序。
- `superpowers/`：采用「specs → plans」的双轨文档结构——`specs/` 存放带日期前缀的 `.md` 设计文档（如 multi-agent、SSE reactor refactor、RAG、MCP、权限中间件、分布式会话、Agentscope 2.0 GA 迁移等），对应 `plans/` 中按相同日期命名的实施计划，均以任务化 check-list 和文件清单描述变更范围，形成设计与实现的一一对应关系。
- 时间线从 2026-04-09 Chat UI 开始，经多 Agent、Pipeline、HITL 审批中间件、Distributed Session、Permission Demo，到 2026-07-10 Agentscope RC3→GA 迁移，覆盖项目主要架构迭代轨迹。
- 命名约定严格遵循 `YYYY-MM-DD-*.md` 加前缀（p0/p1/p2/p3/p4/p5/p6）表达优先级与特性编号，使版本控制下可按日期/优先级检索任一特性的完整上下文。