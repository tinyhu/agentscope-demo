依赖外部 AgentConfig 中 `SharedBlackboardConfig.enabled` 及可选 `storageKey`（默认 `shared_blackboard`），并通过 Spring 配置 `AgentStateStore` Bean 切换为 Redis/MySQL/Postgres 分布式存储；测试通过带 `AgentStateStore` 构造器替换默认 InMemory 实现。