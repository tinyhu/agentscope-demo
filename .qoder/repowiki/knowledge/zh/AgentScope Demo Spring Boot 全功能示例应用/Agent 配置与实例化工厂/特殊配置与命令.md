应用启动时 `AgentConfigService` 自动读取 classpath 下的 `config/agents.yml` 与可选的 `harness-agents.yml`；若指定 `redis`/`mysql`/`postgresql` Spring Profile 会注入分布式 `AgentStateStore`，`AgentFactory.createStateStore` 将优先使用该 bean 而非 InMemory/JsonFile。