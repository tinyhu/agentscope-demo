运行前需设置 `DASHSCOPE_API_KEY` 环境变量；`WebSearchTool` 依赖 Tavily API Key（默认读 `tavily.api-key`，缺省回退至示例 key）。模板文件需放在 classpath `skills/bank_invoice_java/assets/` 下（`bank_template.xlsx`、`bank_template.docx`），否则生成发票会抛出模板未找到异常。Skill 元数据来自 `classpath:skills/*/SKILL.md` 的 YAML frontmatter。