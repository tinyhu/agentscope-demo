构造运行时需要已装配的 `ObservabilityHook`、`ApprovalService`、`ApprovalMiddleware`（HITL 路径），以及通过 `AgentConfigService` 解析的 `AgentConfig`；对于 STATE_GRAPH 型需要额外的 `OrderFulfillmentGraph`；结构化输出运行时还需可反射加载的 schema class 名称。