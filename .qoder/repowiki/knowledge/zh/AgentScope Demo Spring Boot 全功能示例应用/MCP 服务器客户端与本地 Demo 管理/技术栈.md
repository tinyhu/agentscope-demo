AgentScope 2.0 `io.agentscope.core.tool.mcp`（McpClientBuilder/McpClientWrapper）、SnakeYAML（自定义 Constructor 反序列化）、Spring Boot 3（@Service/@Component/@Value/@PostConstruct/@PreDestroy），依赖外部 Node.js 运行时与 `@modelcontextprotocol/server-everything` + `supergateway` 以提供本地可运行的 MCP 服务端。