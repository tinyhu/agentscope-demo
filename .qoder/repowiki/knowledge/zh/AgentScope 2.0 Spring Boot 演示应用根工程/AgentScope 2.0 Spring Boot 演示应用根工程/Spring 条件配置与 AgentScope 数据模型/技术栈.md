Spring Boot 3.5 `@Profile` 条件装配；AgentScope 2.0 `ModelRegistry` + `DashScopeChatFormatter`；Lombok `@Getter`；Jackson `@JsonInclude(NON_NULL)`；Redis JedisPooled / MySQL / PostgreSQL 作为分布式 `AgentStateStore` 后端。