`A2aServerConfig`、`AguiConfig`、`FeishuChannelConfig`、`DistributedStateStoreConfig` 中的 bean 仅在对应 Spring profile 激活时才加载（`--spring.profiles.active=a2a|agui|feishu|redis|mysql|postgresql`），其中分布式 state store 要求额外环境变量 `DASHSCOPE_API_KEY` 或 `agentscope.model.dashscope.api-key`。