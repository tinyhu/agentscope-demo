启用 RAG 需在 `application.yml` 设置 `agentscope.model.dashscope.api-key`，并通过 `agentscope.knowledge.enabled`/`agentscope.knowledge.autoIndexOnStartup` 控制是否在服务启动后自动后台索引知识目录；会话存储基路径由 `agentscope.session.storage-path` 配置（日志显示当前实现仅为 in-memory）。