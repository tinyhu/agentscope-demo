构建: `mvn clean compile`；运行需设置 `DASHSCOPE_API_KEY` 环境变量或通过 `-Dagentscope.model.dashscope.api-key=...` 传入，然后 `mvn spring-boot:run` 即可在 http://localhost:8080 访问聊天 UI。启用扩展需激活 Spring Profile：a2a、feishu、agui、distributed-state-store。