Spring Boot 3.5 组件注解 + Project Reactor `Flux` 响应式流；AgentScope Harness 2.0（`HarnessAgent`、`LocalFilesystemSpec`、`DockerFilesystemSpec`、`NoopSnapshotSpec`、`ClasspathSkillRepository`、`ArtifactDeliveryTarget` SPI）；DashScope API Key 通过 `agentscope.model.dashscope.api-key` 配置项或 `DASHSCOPE_API_KEY` 环境变量注入。