- 通过注入的 `ModelFactory` 和 `AgentConfigService` 解析模型与子代理配置，而非直接 new 实例
- ROUTING/HANDOFFS 模式下将每个子代理以 `SubAgentProvider` + `SubAgentTool` 形式注册到 `Toolkit`，并附带 `StreamOptions` 转发 EVENT 与 TOOL_RESULT 流
- 状态图通过 `addEventConsumer` 回调对外透传 `graph_agent_call` / `graph_transition` 等结构化事件，供上层监控消费
- 无代理状态按用户事件进行确定性跳转，含英文关键词到中文别名（submit/提交、pay/支付 等）的双向映射
- 对用户输入空边界（无状态、null 转场、无匹配事件）均返回带提示信息的友好 `TextBlock` 消息而不抛异常