通过 Maven 构建并运行 `mvn spring-boot:run`，默认启动内嵌 Tomcat；使用 `-Dspring.profiles.active=a2a,agui,feishu,mysql` 等组合 profile 启用不同协议栈；`agent-harness/` 目录作为独立 Python 包可通过 `pip install -e .` 安装后以 `python -m cli_anything.agentscope` 调用。